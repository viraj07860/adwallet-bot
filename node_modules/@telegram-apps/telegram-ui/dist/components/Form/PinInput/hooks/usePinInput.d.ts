export declare enum BiometricType {
    FACEID = "faceid",
    FINGERPRINT = "fingerprint"
}
interface UsePinInputProps {
    pinCount: number;
    value?: number[];
    onChange?(value: number[]): void;
    onBiometricAuth?(): void;
    biometricType?: BiometricType;
}
export declare const DEFAULT_PINS: (string | number)[];
export declare const getAvailablePins: (biometricType?: BiometricType) => (string | number)[];
export declare const usePinInput: ({ pinCount, value: valueProp, onChange, onBiometricAuth, biometricType, }: UsePinInputProps) => {
    value: number[];
    setInputRefByIndex: (index: number, ref: HTMLLabelElement | null) => void;
    onElementClick: (element: string | number) => void;
    handleButton: (index: number, button: string) => void;
    PINS: (string | number)[];
};
export {};
//# sourceMappingURL=usePinInput.d.ts.map